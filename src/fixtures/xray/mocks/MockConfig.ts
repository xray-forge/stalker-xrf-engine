// Re-exported from the `xray16/mocks` package so the engine mocks and the package's server-object mocks share ONE
// id counter instance (both `game_object.mock` here and the package `cse_alife_*` base auto-assign ids from it).
export { mockConfig } from "xray16/mocks";
