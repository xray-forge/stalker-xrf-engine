local LuaLogger = require("scripts.utils.logging").LuaLogger
local create_ts_class_instance = require("lualib_bundle").__TS__New
local log = create_ts_class_instance(LuaLogger, "zone_no_weapon");

----------------------------------------------------------------------------------------------------------------------
-- ����� CZoneNoWeapon. ��� ����� ������ � ����, � ���� ��������� ������. ��� ������ - ���������
----------------------------------------------------------------------------------------------------------------------

class "CZoneNoWeapon"

function CZoneNoWeapon:__init(zone, binder, ini)
  log:info("Init")

  -- ���� ��� ���� ����� � �� � ����, �� ������� ������
  if binder.actor_inside then
    db.actor:hide_weapon()
  end
end

function CZoneNoWeapon:on_enter(actor)
  log:info("Entered no weapon zone")
  actor:hide_weapon()
end

function CZoneNoWeapon:on_leave(actor)
  log:info("Left no weapon zone")
  actor:restore_weapon()
end
