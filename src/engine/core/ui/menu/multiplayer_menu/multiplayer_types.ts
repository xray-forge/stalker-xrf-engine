/**
 * Variant of active sub-tab in multiplayer options.
 */
export enum EMultiplayerMenuTab {
  CLIENT = "client",
  OPTIONS = "options",
  SERVER = "server",
  DEMO = "demo",
  PROFILE = "profile",
}
