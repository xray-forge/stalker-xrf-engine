import { describe, it } from "@jest/globals";

describe("updateSquadMapSpot util", () => {
  it.todo("should correctly update display squad map spots");
});

describe("removeSquadMapSpot util", () => {
  it.todo("should correctly remove map spots");
});
