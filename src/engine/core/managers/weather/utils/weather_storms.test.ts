import { describe, it } from "@jest/globals";

describe("updateDistantStorm util", () => {
  it.todo("should correctly handle storm updates");
});

describe("isInsideBoundaries util", () => {
  it.todo("should correctly check if actor inside boundaries");
});

describe("generateDistantStormLightning util", () => {
  it.todo("should correctly generate thunders");
});
