import { describe, expect, it } from "@jest/globals";

import { getStateIndexByHp, parseWoundedData } from "@/engine/core/schemes/stalker/wounded/utils/wounded_parse";
import { parseConditionsList } from "@/engine/core/utils/ini";
import { MockLuaTable } from "@/fixtures/lua";

describe("processHPWound util", () => {
  it.todo("should correctly process values");
});

describe("processPsyWound util", () => {
  it.todo("should correctly process values");
});

describe("processVictim util", () => {
  it.todo("should correctly process values");
});

describe("processFight util", () => {
  it.todo("should correctly process values");
});
