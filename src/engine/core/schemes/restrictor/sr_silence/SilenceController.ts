import { AbstractSchemeController } from "@/engine/core/schemes/base";
import { ISchemeSilenceState } from "@/engine/core/schemes/restrictor/sr_silence/sr_silence_types";

/**
 * Empty placeholder controller.
 */
export class SilenceController extends AbstractSchemeController<ISchemeSilenceState> {
  // Empty.
}
