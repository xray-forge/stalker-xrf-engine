import { describe, it } from "@jest/globals";

describe("ActionAnimpoint class", () => {
  it.todo("should correctly perform animpoint action");

  it.todo("should correctly handle scheme events");
});
